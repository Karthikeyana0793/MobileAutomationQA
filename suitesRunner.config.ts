import { SPECPATH } from './constant/helper.constant';

export const SUITES = {
    smoke: [
        // SPECPATH + 'carrierCreation' + '.ts',
        // SPECPATH + 'search' + '.ts',
    ],
    regression: [
        // ...
    ],
};
